# Large context file (optional)

The original slide deck file is very large (~166 MB) and is not included in the zip bundle.

If you want Codex to read it, either:
- add it with Git LFS, or
- export a text/PDF version and commit that instead.

Original filename (local): `РазвитиеДляРазвитых_1feb26.pptx`
