You are running as a scheduled automation.

Rules:
- Do at most one "slice" of work (~10–15 min).
- Make small, reviewable diffs.
- End the slice by writing exactly one checkpoint + updating `work/STATE.md` (prefer `$checkpoint` if supported; otherwise follow `.codex/skills/checkpoint/SKILL.md`), then stop.
- If the platform shows any usage-limit warning or asks to buy credits or blocks on permissions:
  still write the checkpoint + update `work/STATE.md`, then stop.

Work:
1) Read latest checkpoint.
2) Pick the single highest-leverage next action.
3) Execute it (e.g., add 2–5 high-quality sources + update `work/evidence_pack.md`, or refine one hypothesis + falsification test).
4) Update any affected deliverable files.
5) Write the checkpoint + update `work/STATE.md` (prefer `$checkpoint` if supported; otherwise follow `.codex/skills/checkpoint/SKILL.md`).
