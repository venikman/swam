Resume the memetics sprint from repo state.

1) Read `work/STATE.md` and summarize current state.

2) Read the newest file in `work/checkpoints/` and summarize delta since `work/STATE.md`.

3) Read `work/00_deliverables_manifest.md` and list what's missing/incomplete.

4) Continue work, producing the next checkpoint file and updating deliverables + `work/STATE.md`.

Follow all rules in `prompts/00_codex_thread_prompt.md`.
